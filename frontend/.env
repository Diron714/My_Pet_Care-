# Local API (include /api). Production: set to your deployed API URL.
VITE_API_BASE_URL=http://localhost:5000/api
# Optional: if Socket.IO is on a different origin than the API host above.
# VITE_SOCKET_URL=http://localhost:5000
